<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/btn_round_color"/>
    <corners android:radius="0dp" />
    <stroke
        android:color="@color/btn_round_color" />
</shape>